class Program{
	struct A
	{
		int b;
	};
	struct B
	{
		int b;
		struct A c;
	};
	int variable;
	struct B y;
	int arreglo[5];

	struct A T[10];
	int main()
	{
		int variableInt;
		boolean variableBool;
		char variableChar;

		variableInt = 5;
		variable = variableInt * 3;
		variable = variableInt - 3;

		variableChar = 'a';
		variableBool = true;
		arreglo[5] = (4/3) - (1+2);

		variableBool = (1<variableInt) || (variableChar == 'a')

	}

}