class Program{
	struct A
	{
		int a;
	};
	struct B
	{
		int b;
		struct A c;
	};
	int variable;
	struct B y;
	int arreglo[5];

	struct A T[10];
	boolean test(int a )
	{

	}
	int main(int a, int b, void)
	{
		int variableInt;
		boolean variableBool;
		char variableChar;
		struct B y;

		variableInt = 5;
		variable = variableInt * 3;
		variable = variableInt - 3;

		y.c.a = 1 + y.c.a;


		while (true)
		{
			variableChar = 'a';
			variableBool = test(variableInt);
			arreglo[5] = (4/3) - (1+2);
		}


		variableBool = (1<variableInt) || (variableChar == 'a')
		return 1 + variableInt;
	}

}